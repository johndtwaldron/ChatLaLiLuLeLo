# ChatLaLiLuLeLo Mobile App Environment Configuration

# Backend API URL
# For local development: http://localhost:8787
# For production: https://your-worker-subdomain.your-username.workers.dev
EXPO_PUBLIC_API_URL=http://localhost:8787

# App Configuration
EXPO_PUBLIC_APP_VERSION=1.0.0
EXPO_PUBLIC_SESSION_ID_PREFIX=chatlali

# Development settings
EXPO_PUBLIC_DEBUG_MODE=true

# Voice/TTS Configuration (DISABLED BY DEFAULT FOR SECURITY)
# Enable voice features (false by default for safety)
VOICE_ENABLED=false

# TTS Engine Selection (openai|elevenlabs|coqui)
# Only used if VOICE_ENABLED=true and respective API key is present
VOICE_ENGINE=openai

# OpenAI TTS API Key (for OpenAI TTS engine)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# ElevenLabs Configuration (feature-flagged, disabled by default)
ELEVENLABS_ENABLED=false
ELEVENLABS_API_KEY=

# Coqui Local TTS Configuration (for local XTTS/Piper setup)
COQUI_ENABLED=false
COQUI_TTS_URL=http://localhost:8020/tts

# Voice Settings
VOICE_AUTOPLAY=false
VOICE_VOLUME=0.7
VOICE_PRESET=colonel-neutral
VOICE_SFX=true

# Web Preview Safety (force disable voice on preview deployments)
# Set PAGES_VOICE_ENABLE=1 in deployment secrets to override
# PAGES_VOICE_ENABLE=1
