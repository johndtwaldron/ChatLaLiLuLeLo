name: Test Dev+ Branch Trigger

on:
  push:
    branches: 
      - "Dev+"
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test Dev+ branch workflow
        run: |
          echo "🧪 Testing Dev+ branch workflow trigger"
          echo "Branch: ${{ github.ref_name }}"
          echo "Event: ${{ github.event_name }}"
          echo "SHA: ${{ github.sha }}"
          echo "✅ Dev+ workflow triggered successfully!"
